- To compile the sources, you need to have the following installed:
1. the 'xelatex' TeX engine
2. the 'minted' package (pygmentize, etc...)

- Then, run the following command (at least twice):
$ xelatex -shell-escape dyck.tex

